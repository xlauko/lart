{
	"name": "LART Developement",

	"context": "..",

	"dockerFile": "./Dockerfile",

	"settings": {
		"terminal.integrated.shell.linux": "/bin/bash",
		"cmake.cmakePath": "/usr/opt/cmake/bin/cmake"
	},

	"extensions": [
		"ms-vscode.cpptools", // C++
		"matepek.vscode-catch2-test-adapter", // Test explorer
		"ms-vscode.cmake-tools" // CMake
	],

	"runArgs": [
		"--cap-add=SYS_PTRACE",
		"--security-opt",
		"seccomp=unconfined"
	]
}
